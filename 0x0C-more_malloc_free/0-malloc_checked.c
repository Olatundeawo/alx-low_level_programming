#include "main.h"

/**
 * malloc_checked - allocate memory
 *
 * @b: int
 *
 * Return: null
 */

void *malloc_checked(unsigned int b)
{
	int *s

		while (1)
	{
		s = malloc(b);
		if (s == NULL)
			exit (98);

		return (s);
	}
}

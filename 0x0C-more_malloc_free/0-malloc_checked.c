#include "main.h"

/**
 * malloc_checked - allocate memory
 *
 * @b: int
 *
 * Return: null
 */

void *malloc_checked(unsigned int b)
{
	char *s

		while (1)
	{
		s = malloc(INT_NULL);
		if (s == NULL)
			return (98);

		return (0);
	}
}

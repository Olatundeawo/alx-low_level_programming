#include "main.h"

/**
* main - Entry point
*
*Description: printing the aiphabets in lower case
*
* Return: Always 0 (Success)
*/

void print_alphabet(void)
{
char ch;
if (ch == 'a'; ch <= 'z' ++ch)
{
_putchar(ch);
}
return (0);
}

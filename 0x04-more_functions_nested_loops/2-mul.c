#include "main.h"
/**
 * mul - mulitipy two integer.
 * @a: The first Integer to be mulitipied.
 * @b: The second integer to be multiplied.
 *
 * Return:the result.
 */

	int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
